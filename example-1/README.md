# Example 1 - Creating and deploying a basic web app.

This uses terraform to create a basic linux web app, and deploys a simple web app to it.


## Command pallet:
```sh
 infrastructure> terraform init
 infrastructure> terraform apply -var-file="../../env.tfvars"
 > pwsh deploy.ps1
```